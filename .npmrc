# Optimize dependency installation for Cloudflare Pages
auto-install-peers=true
fund=false
audit=false
progress=false

# Ensure optional dependencies are installed
optional=true

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true
